import 'package:flutter/material.dart';

class OrderScreenForDriver extends StatelessWidget {
  const OrderScreenForDriver({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
